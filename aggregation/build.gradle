r {
    interpreter = getRInterpreter()
    src = './aggregation/src/main/R/JConf/*'
}

static def getRInterpreter() {
    if (System.env['R_BIN'] != null) {
        "${System.getenv('R_BIN')}"
    } else {
        '/usr/local/bin/R'
    }
}
