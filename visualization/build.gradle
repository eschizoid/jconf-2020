r {
    interpreter = getRInterpreter()
    src = './visualization/src/main/R/ChicagoCloudConference/*'
}

def getRInterpreter() {
    if (System.env['R_BIN'] != null) {
        "${System.getenv('R_BIN')}"
    } else {
        '/usr/local/bin/R'
    }
}
